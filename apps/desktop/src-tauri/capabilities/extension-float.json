{
  "$schema": "https://scala.tauri.app/config.schema.json",
  "identifier": "extension-float",
  "description": "Detached Extension Float windows. A Float is a thin client: it renders one presentation slot, talks only to the main window over app events, and never reaches the Host transport, the settings store, the shell, the browser surface, dialogs, or the updater. Clipboard text is allowed because a detached custom() panel is a real terminal, and the clipboard is the user's own, not a channel into PiDeck state.",
  "windows": ["pideck-float-*"],
  "remote": {
    "urls": ["http://127.0.0.1:1420/*"]
  },
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging",
    "core:window:allow-outer-size",
    "core:window:allow-outer-position",
    "core:window:allow-scale-factor",
    "core:window:allow-set-theme",
    "clipboard-manager:allow-read-text",
    "clipboard-manager:allow-write-text"
  ]
}
